import {prisma} from '@/test/setup'
import {beforeEach, describe, expect, it, test, vi} from 'vitest'


describe('AnalyticsService', () => {
    beforeEach(() => {
        const projectInput = {

        }
    })


    describe('getAnalyticsSummary', () => {
        it('Should return an analyticsSummary', () => {

        })
    })

})